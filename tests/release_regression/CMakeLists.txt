cmake_minimum_required(VERSION 3.24)
project(RTXMFGReleaseRegression LANGUAGES C CXX)
set(NATIVE_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/../../source/native" CACHE PATH "Release native source")
set(STREAMLINE_ROOT "" CACHE PATH "Pinned Streamline SDK 2.14.1")
set(VULKAN_INCLUDE_DIR "" CACHE PATH "Pinned Vulkan SDK headers")
set(VULKAN_LIBRARY "" CACHE FILEPATH "Pinned Vulkan loader import library")
add_executable(DynamicAvailabilityTests dynamic_availability_tests.cpp)
target_compile_features(DynamicAvailabilityTests PRIVATE cxx_std_20)
target_include_directories(DynamicAvailabilityTests PRIVATE "${NATIVE_SOURCE}")
target_compile_definitions(DynamicAvailabilityTests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
set_target_properties(DynamicAvailabilityTests PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
add_executable(StartupDefaultsTests startup_defaults_tests.cpp)
target_compile_features(StartupDefaultsTests PRIVATE cxx_std_20)
target_include_directories(StartupDefaultsTests PRIVATE "${NATIVE_SOURCE}")
target_compile_definitions(StartupDefaultsTests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
set_target_properties(StartupDefaultsTests PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
foreach(target VulkanMenuTests VulkanDynamicMenuTests VulkanStreamlineMenuTests VulkanMixedMenuTests)
    add_executable(${target} vulkan_menu_tests.cpp)
    target_compile_features(${target} PRIVATE cxx_std_20)
    target_include_directories(${target} PRIVATE "${NATIVE_SOURCE}" "${VULKAN_INCLUDE_DIR}")
    target_include_directories(${target} PRIVATE "${STREAMLINE_ROOT}/include")
    target_compile_definitions(${target} PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN VK_USE_PLATFORM_WIN32_KHR=1)
    set_target_properties(${target} PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endforeach()
target_link_libraries(VulkanMenuTests PRIVATE "${VULKAN_LIBRARY}")
target_compile_definitions(VulkanDynamicMenuTests PRIVATE VK_NO_PROTOTYPES=1 MFG_VK_DYNAMIC=1)
target_compile_definitions(VulkanStreamlineMenuTests PRIVATE VK_NO_PROTOTYPES=1 MFG_VK_DYNAMIC=1 MFG_VK_STREAMLINE=1)
target_compile_definitions(VulkanMixedMenuTests PRIVATE VK_NO_PROTOTYPES=1 MFG_VK_DYNAMIC=1 MFG_VK_STREAMLINE=1 MFG_VK_MIXED=1)
add_executable(ProviderGateTests provider_gate_tests.cpp "${NATIVE_SOURCE}/dlssg_provider_policy.cpp")
target_compile_features(ProviderGateTests PRIVATE cxx_std_20)
target_include_directories(ProviderGateTests PRIVATE "${NATIVE_SOURCE}")
target_compile_definitions(ProviderGateTests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(ProviderGateTests PRIVATE version d3d12 dxgi)
set_target_properties(ProviderGateTests PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")

add_executable(NvidiaPolicyTests nvidia_policy_tests.cpp "${NATIVE_SOURCE}/nvidia_mfg_policy.cpp")
target_compile_features(NvidiaPolicyTests PRIVATE cxx_std_20)
target_include_directories(NvidiaPolicyTests PRIVATE "${NATIVE_SOURCE}")
target_compile_definitions(NvidiaPolicyTests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
set_target_properties(NvidiaPolicyTests PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")

add_library(ControlWrapperFixture SHARED control_wrapper_fixture.cpp)
target_compile_features(ControlWrapperFixture PRIVATE cxx_std_20)
target_include_directories(ControlWrapperFixture PRIVATE "${STREAMLINE_ROOT}/include")

add_executable(ControlResolverTests control_resolver_tests.cpp)
target_compile_features(ControlResolverTests PRIVATE cxx_std_20)
target_compile_definitions(ControlResolverTests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN MFG_UNLOCK_RUNTIME_GPU_SELECTION=1)
target_include_directories(ControlResolverTests PRIVATE "${NATIVE_SOURCE}" "${STREAMLINE_ROOT}/include")
set_target_properties(ControlWrapperFixture ControlResolverTests PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")

add_library(SlInitFixture SHARED slinit_fixture.cpp slinit_fixture.def)
target_compile_features(SlInitFixture PRIVATE cxx_std_20)
target_compile_definitions(SlInitFixture PRIVATE SL_INTERPOSER UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_include_directories(SlInitFixture PRIVATE "${STREAMLINE_ROOT}/include")
set_target_properties(SlInitFixture PROPERTIES OUTPUT_NAME "sl.interposer" MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")

add_executable(ReleaseLoadCheck loader_tests.cpp)
target_compile_features(ReleaseLoadCheck PRIVATE cxx_std_20)
target_include_directories(ReleaseLoadCheck PRIVATE "${NATIVE_SOURCE}" "${CMAKE_CURRENT_SOURCE_DIR}")
target_compile_definitions(ReleaseLoadCheck PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN MFG_UNLOCK_RUNTIME_GPU_SELECTION=1)
target_link_libraries(ReleaseLoadCheck PRIVATE dinput8 dxguid)
set_target_properties(ReleaseLoadCheck PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")

add_library(ExistingOverlayFixture SHARED overlay_fixture.cpp
    "${NATIVE_SOURCE}/third_party/minhook/src/hook.c"
    "${NATIVE_SOURCE}/third_party/minhook/src/buffer.c"
    "${NATIVE_SOURCE}/third_party/minhook/src/trampoline.c"
    "${NATIVE_SOURCE}/third_party/minhook/src/hde/hde64.c")
target_include_directories(ExistingOverlayFixture PRIVATE "${NATIVE_SOURCE}/third_party/minhook/include")
target_compile_features(ExistingOverlayFixture PRIVATE cxx_std_20)
target_compile_definitions(ExistingOverlayFixture PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(ExistingOverlayFixture PRIVATE d3d12 dxgi user32)
set_target_properties(ExistingOverlayFixture PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")

add_executable(WarpPresentHarness warp_present_harness.cpp)
target_compile_features(WarpPresentHarness PRIVATE cxx_std_20)
target_include_directories(WarpPresentHarness PRIVATE "${NATIVE_SOURCE}" "${CMAKE_CURRENT_SOURCE_DIR}")
target_include_directories(WarpPresentHarness PRIVATE "${STREAMLINE_ROOT}/include")
target_compile_definitions(WarpPresentHarness PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(WarpPresentHarness PRIVATE d3d12 dxgi user32 dwmapi)
set_target_properties(WarpPresentHarness PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
